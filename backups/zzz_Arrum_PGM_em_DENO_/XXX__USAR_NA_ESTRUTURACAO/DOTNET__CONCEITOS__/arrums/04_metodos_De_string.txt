modelo:
ferramenta:
    metodo: , 
    uso: 
    exemplo: 

---

metodos_de_string
 conceito: se clicar nos metodos irá para o [metadata do tipo] e na classe dele vera todos os metodos disponiveis e como usalos
 importante: quando uma pesquisa NaoBoleana de retorno ..  nao existir no alvo pode retornar -1

    ferramenta: comparar_strings
    metodo: Equals() retorna bool True ou False , 
    uso: pode desencadear de uma string ou passar a ( string, comparacao )
    exemplo:
    var texto = "texto1";
    Console.WriteLine(Equals(texto, "texto1"));
    Console.WriteLine(texto.Equals("texto1"));
    
---
    ferramenta: comparar_strings ignorando caseSensitive Maiusculo Minusculo
    metodo: Equals() retorna bool True ou False , 
    uso: usando o terceiro param do metodo Equals com StringComparison.OrdinalIgnoreCase

    exemplo:
    var texto = "texto1";
    var comparacao = string.Equals(texto, "Texto1", StringComparison.OrdinalIgnoreCase);
    Console.WriteLine( comparacao );
---

diversos_metodos_de_string:
    texto.Contains('e')  // Contains de contem "TEM" algo no seu valor.
    ---

     texto.Contains('i', StringComparison.OrdinalIgnoreCase) 
      ); 
      /* 
      ferramenta: comprando se contem ignorando caseSensitive 
      copiado_doc :: Contains(char value, StringComparison comparisonType)
      troca: har value >>  pelo que procura literalmenteMagico,  comparisonType >> pelo membroFerramenta que quer usar do tipo apontado antes.
      */
      ---

    diversos_metodos_de_string:

    Console.WriteLine(texto.EndsWith('1')); // termina com isto ?
    Console.WriteLine(texto.StartsWith('1')); // comeca com isto ?
    Console.WriteLine(texto.IndexOf('x')); // indicePosicao deste ?

    // obs: metodos staticos do tipo - chame pelo tipo e desencadeie o metodo geralmente Is algo... e passe a sua stringTargetAlvo
    Console.WriteLine(string.IsNullOrWhiteSpace(texto)); // é nulo ou tem espaço vazio este metodo é o melhor para espaços vazios?

    Console.Write( string.Join('*', colecaoNomes)); // junta textos , pede um char para ser o separador e um array

    ---


